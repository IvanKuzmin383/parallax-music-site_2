import type { ReactNode } from "react"

/** Сегмент /s без client-shell — только смартлинки. */
export default function SmartlinkSegmentLayout({ children }: { children: ReactNode }) {
  return children
}
